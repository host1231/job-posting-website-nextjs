/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "vuxz9tznczckbg5g.public.blob.vercel-storage.com"
        ]
    }
};

export default nextConfig;
